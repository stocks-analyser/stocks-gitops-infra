apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgres-db
  namespace: argocd
spec:
  source:
    helm:
      valuesFrom:
        - configMapKeyRef:
            name: postgres-db-config
            key: values.yaml
    